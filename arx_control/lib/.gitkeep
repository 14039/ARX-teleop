# This file ensures the lib directory structure is preserved in git
# Actual library files (.so, .dylib, .dll) are not tracked
# Run ./setup_libraries.sh to download/install the libraries for your platform